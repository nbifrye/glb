package gitlabop

// DefaultPerPage is the default number of items per page for list operations.
const DefaultPerPage = 20

// DefaultState is the default state filter for issues and merge requests.
const DefaultState = "opened"
